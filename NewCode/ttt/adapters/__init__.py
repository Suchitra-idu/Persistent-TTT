"""Ring 3 — concrete implementations of the ports, real and fake side by side.

The fake is a first-class deliverable, not a test afterthought: it is what
lets the whole of Ring 4 run in CI with no GPU.
"""
