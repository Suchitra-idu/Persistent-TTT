"""Ring 5 — append-only compositions.

Each experiment is self-contained and composes the inner rings. Never
refactor an old experiment to share code with a new one: add ``_v2``.
Nothing imports this ring.
"""
