"""Ring 1 — the science that varies, as plugins behind registries.

Imports: ``ttt.core`` only. One file per plugin; each registry has one
contract suite that every registered plugin auto-enrols in.
"""
